import { InputErrorsDirective } from './input-errors.directive';

describe('InputErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
