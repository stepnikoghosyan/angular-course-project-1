import { MultilineDirective } from './multiline.directive';

describe('MultilineDirective', () => {
  it('should create an instance', () => {
    const directive = new MultilineDirective();
    expect(directive).toBeTruthy();
  });
});
