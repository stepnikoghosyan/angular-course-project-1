import { SetUserImagePipe } from './set-user-image.pipe';

describe('SetUserImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SetUserImagePipe();
    expect(pipe).toBeTruthy();
  });
});
